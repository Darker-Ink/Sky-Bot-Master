const { MessageEmbed } = require("discord.js");

module.exports = {
    name: 'example1',
    description: null,
    usage: null,
    category: null,
    aliases: null,
    run: async (client, message, args, config) => {
    }}