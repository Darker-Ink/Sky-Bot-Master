const { MessageEmbed } = require("discord.js");

module.exports = {
    name: 'example3',
    description: null,
    usage: null,
    category: null,
    aliases: null,
    run: async (client, message, args, config) => {
    }}